[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

# Netlify detects Next.js and applies its maintained OpenNext adapter. Keep the
# .next output so the server-side API route is deployed as a function.
